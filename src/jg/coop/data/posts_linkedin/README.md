# LinkedIn Posts

LinkedIn posts archive. The `jg sync posts-linkedin` command downloads the latest dataset from the `supreme_coder/linkedin-post` Apify Actor and saves each post as a JSON file in this directory.

The JSON files are kept up-to-date in the CI. Changes are automatically commited back to the repository.